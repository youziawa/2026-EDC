# 小车与地面站开发计划

适用题目：D题《陆空协同无人机系统》
负责人：小车、地面站、小车视觉
当前版本：V0.2
统一状态机：`陆空协同统一状态机.md`
AI规范：`land_air_state_machine.yaml`

## 1. 当前目标

完成小车循线、里程定位、C—D动作区减速、任务事件上报，以及运行在电脑上的PyQt地面站。

统一任务关系：

```text
小车K230视觉
      ↓ VISION_LINE
小车主控 STM32F407VET6
      ↓ ECB02从机
飞机主控（蓝牙主机、全局状态权威）
      ↓ ECB02从机
电脑PyQt地面站
```

飞机负责全局任务状态和通信路由。小车与地面站不得各自推断或推进全局状态。

## 2. 负责范围

### 2.1 小车

- TB6612双路电机驱动；
- MG310MGR电机AB相编码器采集；
- 左右轮速度闭环和差速循线；
- 小车K230黑线视觉；
- 里程计及A/B/C/D点映射；
- C点自动减速、D点自动退出慢速；
- 一键启动和 `run_id` 管理；
- `TASK_START`、`CAR_STATE`、`CAR_POSE`、`TRACK_EVENT` 遥测；
- 一圈完成后的A点横线确认和停车；
- 丢线分级恢复和安全停车。

### 2.2 PyQt地面站

- 通过USB-UART连接ECB02从机；
- 解析飞机转发的LXS1消息；
- 显示场地、飞机、小车、轨迹和C—D动作区；
- 显示全局状态、小车状态、动作状态和故障；
- 保存CSV或JSON日志；
- 统计链路超时和丢帧；
- 发送查询和紧急中止。

### 2.3 不在本计划内直接实现

- 天空端K230的小车和平台目标识别；
- 飞机飞控控制器和抛投机构；
- 凌霄飞控原厂协议适配；
- ECB02内部固件开发；
- 视频传输。

这些模块必须遵循统一状态机和LXS1接口完成联调。

## 3. 已确认硬件基线

| 模块 | 设备 | 接口 |
|---|---|---|
| 小车主控 | 天空星 STM32F407VET6 | 定时器、PWM、UART、DMA、SWD |
| 电机驱动 | 成品TB6612模块 | PWMA/PWMB、AIN1/AIN2、BIN1/BIN2、STBY |
| 驱动电机 | MG310MGR编码器电机 | 左右各A/B两相信号 |
| 小车视觉 | 亚博K230 | USART3，LXS1 `VISION_LINE` |
| 小车无线 | ECB02从机 | USART2，115200 8N1 |
| 地面站 | Windows电脑 + PyQt | USB-UART + ECB02从机 |
| 通信主机 | 飞机主控 | ECB02主机 |

当前小车引脚：

| 功能 | STM32引脚 |
|---|---|
| 左编码器 | PA0、PA1 / TIM2 |
| 右编码器 | PB6、PB7 / TIM4 |
| 右电机PWM | PA8 / TIM1_CH1 |
| 右电机方向 | PB0、PB1 |
| 左电机PWM | PB15 / TIM12_CH2 |
| 左电机方向 | PB12、PB13 |
| TB6612 STBY | PB14 |
| 地面站/无线串口 | PA2、PA3 / USART2 |
| 小车K230串口 | PB10、PB11 / USART3 |

TB6612控制原则：

```text
正转：方向脚设置正向，PWM输出占空比
反转：方向脚设置反向，PWM输出占空比
滑行停止：PWM=0，方向脚为低
驱动待机：STBY=0
驱动使能：STBY=1
```

## 4. 任务模式与状态

任务模式使用编译常量：

```text
1 DROP             抛投任务
2 DYNAMIC_LANDING  动态起降任务
```

飞机全局流程：

```text
抛投：
IDLE → TAKEOFF → HOVER_3S → SEARCH → FOLLOW
→ DROP → RETURN → LAND_H → DONE

动态起降：
IDLE → TAKEOFF → SEARCH → FOLLOW
→ LAND_CAR → WAIT_5S → RETAKEOFF
→ RETURN → LAND_H → DONE
```

`FOLLOW` 负责伴飞并等待C点，不单独设置 `ACTION_WAIT_C`。

小车流程：

```text
READY → STARTING → NORMAL_TRACK
                    ↓ C_ENTER
                 ACTION_SLOW
                    ↓ RETURN或D_EXIT
                 NORMAL_TRACK
                    ↓ A_FINISH
               FINISH_CONFIRM → DONE
```

丢线流程：

```text
NORMAL_TRACK/ACTION_SLOW
→ LOST_HOLD
→ REACQUIRE
→ 返回丢线前状态

REACQUIRE超时 → FAULT
```

## 5. 赛道里程和速度

理论里程初值：

| 位置 | 累计里程 |
|---|---:|
| B点 | 1500 mm |
| C点 | 3856 mm |
| D点 | 5356 mm |
| 一圈 | 7712 mm |

统一配置参数：

```yaml
normal_speed_mm_s: 150
action_speed_mm_s: 80
reacquire_speed_mm_s: 40
point_b_path_mm: 1500
point_c_path_mm: 3856
point_d_path_mm: 5356
lap_path_mm: 7712
```

规则：

1. 小车从A点启动后以正常速度循线。
2. 到达C点后立即自主减速，不等待飞机或地面站确认。
3. C—D区间周期发送 `C_ENTER`。
4. 抛投完成后，飞机进入 `RETURN`，小车恢复正常速度。
5. 动态任务完成平台再起飞后，飞机进入 `RETURN`，小车恢复正常速度。
6. 若飞机链路失效，小车在D点或慢速超时后自主恢复正常速度。
7. 所有速度切换使用斜坡，动作区内不得停车。
8. A点停车需要“一圈最小里程＋40 cm横线识别”同时成立。

## 6. LXS1接口

### 6.1 小车K230 → 小车F407：VISION_LINE

```text
valid:u8
lost_count:u8
confidence:u16
lateral_error_mm:i16
heading_error_deg:i16
curvature:i16
```

发送频率：20～50 Hz。超过视觉超时后不得继续使用旧误差。

### 6.2 小车F407 → 飞机：TASK_START

```text
run_id:u8
task_mode:u8
normal_speed_mm_s:u16
action_speed_mm_s:u16
```

小车按键后发送。相同 `run_id` 的重复帧不得导致飞机重复起飞。

### 6.3 飞机 → 小车和地面站：TASK_STATE

```text
run_id:u8
task_mode:u8
global_state:u8
result:u8
fault_code:u16
elapsed_ms:u32
car_path_mm:u32
```

飞机周期发送。小车以 `global_state=RETURN` 作为正常恢复速度条件。

### 6.4 小车F407 → 飞机：CAR_STATE

```text
run_id:u8
car_state:u8
speed_mode:u8
line_valid:u8
speed_mm_s:u16
fault_code:u16
path_mm:u32
lap:u8
```

### 6.5 小车F407 → 飞机：CAR_POSE

```text
run_id:u8
x_mm:i16
y_mm:i16
heading_deg:i16
path_mm:u32
```

### 6.6 小车F407 → 飞机：TRACK_EVENT

```text
run_id:u8
event:u8
path_mm:u32
```

事件编号：

```text
1 B_CROSS
2 C_ENTER
3 D_EXIT
4 A_FINISH
```

飞机将小车状态、位置和事件原帧转发给地面站，保留 `SRC=CAR`。

## 7. 蓝牙通信计划

逻辑拓扑：

```text
飞机主控（主机）
├─ 小车ECB02从机
└─ 电脑ECB02从机
```

验证顺序：

1. 验证ECB02角色配置、自动重连、连接通知和115200串口透传。
2. 验证一块主机模块能否同时维持两个从机连接。
3. 验证主机能否区分来源并向指定从机发送。
4. 验证任务消息100 ms周期下的延迟和丢包。
5. 若一主两从不成立，飞机安装两块ECB02，分别连接小车和地面站。

无论使用一块还是两块飞机端模块，上层LXS1地址和状态机保持不变。

## 8. PyQt地面站架构

建议目录：

```text
GroundStation/
├─ main.py
├─ ui/
│  └─ main_window.py
├─ protocol/
│  ├─ lxs1_parser.py
│  └─ messages.py
├─ workers/
│  └─ serial_worker.py
├─ models/
│  └─ mission_model.py
├─ widgets/
│  └─ field_view.py
└─ tests/
```

线程模型：

```text
PyQt主线程
├─ 场地图和轨迹绘制
├─ 状态和告警
└─ 查询与紧急中止

串口工作线程
├─ pyserial或QSerialPort
├─ LXS1增量解析
├─ 设备超时判断
└─ 日志队列
```

第一版界面：

- 400 cm × 500 cm场地图；
- A/B/C/D/H点和C—D动作区；
- 飞机和小车实时位置及历史轨迹；
- 全局状态、小车状态和速度模式；
- 飞机高度、速度和视觉置信度；
- 距C/D点路程和动作区剩余时间；
- 蓝牙在线状态、最后收包时间和丢帧统计；
- 抛投、着陆、停留和再起飞结果；
- CSV/JSON日志；
- 查询和紧急中止按钮。

地面站离线时，飞机和小车必须继续任务。

## 9. 开发阶段

### P0：K230视觉与协议基线

- [x] QVGA灰度视觉；
- [x] 多ROI黑线识别；
- [x] 生成 `VISION_LINE`；
- [x] 协议字节测试；
- [ ] 增加A点横线事件；
- [ ] 实车光照和阴影测试。

验收：视觉稳定输出20 Hz以上，丢线时立即 `valid=0`，不输出旧控制量。

### P1：电机与编码器闭环

- [x] TB6612引脚配置；
- [x] 双编码器定时器配置；
- [x] 左右轮速度闭环骨架；
- [x] VOFA诊断；
- [ ] 完成150 mm/s速度标定；
- [ ] 验证80 mm/s动作慢速；
- [ ] 验证速度斜坡。

验收：正常速度和动作速度均稳定，切换时无冲击和停车。

### P2：循线与丢线恢复

- [x] 基于横向、航向和曲率的差速循线；
- [x] 短时丢线保持；
- [ ] 长时低速重寻；
- [ ] 恢复到丢线前速度状态；
- [ ] 最终超时故障停车；
- [ ] 连续10圈稳定测试。

### P3：里程计和赛道事件

- [ ] 标定左右轮每计数路程；
- [ ] 标定有效轮距；
- [ ] A点启动清零；
- [ ] 标定B/C/D累计里程；
- [ ] 发送 `B_CROSS/C_ENTER/D_EXIT`；
- [ ] A点横线和最小圈长双条件；
- [ ] 发送 `CAR_POSE`。

验收：连续多圈中C/D触发位置稳定，不能在启动位置误判完成。

### P4：小车任务状态机

- [ ] 增加物理启动按键；
- [ ] 编译时固定任务模式；
- [ ] `run_id`递增和幂等；
- [ ] C点自主减速；
- [ ] `RETURN`、D点和超时三种恢复条件；
- [ ] 一圈结束停车；
- [ ] 中止和故障状态。

验收：不依赖地面站即可完成正常速度→动作慢速→正常速度→A点停车。

### P5：ECB02链路

- [ ] 小车从机配置；
- [ ] 电脑从机配置；
- [ ] 飞机一主两从可行性测试；
- [ ] 必要时改为飞机双ECB02；
- [ ] 自动重连；
- [ ] LXS1双向透传；
- [ ] 飞机转发小车遥测；
- [ ] 链路故障注入。

### P6：PyQt地面站

- [ ] 串口选择和连接；
- [ ] LXS1增量解析；
- [ ] 场地和轨迹显示；
- [ ] 三端状态显示；
- [ ] C—D动作区显示；
- [ ] 设备超时和丢帧统计；
- [ ] CSV/JSON日志；
- [ ] 紧急中止；
- [ ] PyQt离线打包。

### P7：系统联调

- [ ] 小车按键启动并发送 `TASK_START`；
- [ ] 飞机发布权威 `TASK_STATE`；
- [ ] B点在15秒内到达；
- [ ] C点全体进入抛投/降落阶段；
- [ ] 小车C点减速；
- [ ] 动作完成后恢复速度；
- [ ] D点超时取消动作；
- [ ] 飞机返回H点；
- [ ] 小车一圈不超过90秒；
- [ ] 地面站离线不影响任务；
- [ ] 重复消息不重复触发动作。

## 10. 验收清单

- [ ] 两种任务流程均与统一状态机一致；
- [ ] 不存在 `ACTION_WAIT_C` 状态；
- [ ] 小车在C—D区间只减速、不停车；
- [ ] 飞机从 `SEARCH/FOLLOW` 收到 `C_ENTER` 后进入动作状态；
- [ ] 抛投完成或平台再起飞完成后恢复正常速度；
- [ ] 越过D点未完成时取消动作并恢复速度；
- [ ] 地面站只显示、记录、查询和中止；
- [ ] 所有副作用消息按 `run_id` 幂等；
- [ ] Markdown、YAML、C、Python和PyQt使用相同状态编号；
- [ ] 设计报告记录实际速度、位置误差、任务时间和故障测试结果。
