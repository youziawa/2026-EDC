# 小车与地面站开发计划

适用题目：D题《陆空协同无人机系统》
负责人：小车、地面站、小车视觉
当前版本：V0.3
统一状态机：`陆空协同统一状态机.md`
AI规范：`land_air_state_machine.yaml`

## 1. 当前目标

完成小车循线、里程定位、C—D动作区减速、任务事件上报，以及运行在电脑上的PyQt地面站。

统一任务关系：

```text
小车K230视觉
      ↓ VISION_LINE
小车主控 STM32F407VET6
      ├─ USART2 + ECB02 ↔ 飞机主控
      └─ USART1 + ECB02 ↔ 电脑PyQt地面站
飞机主控
      └─ 第二块ECB02 ↔ 电脑PyQt地面站
```

飞机负责全局任务状态，但不承担小车遥测路由。三个设备各使用两块ECB02，组成飞机—小车、飞机—地面站和小车—地面站三条点对点链路。小车与地面站不得各自推断或推进全局状态。

## 2. 负责范围

### 2.1 小车

- TB6612双路电机驱动；
- MG310MGR电机AB相编码器采集；
- 左右轮速度闭环和差速循线；
- 小车K230黑线视觉；
- 里程计及A/B/C/D点映射；
- C点自动减速、D点自动退出慢速；
- 一键启动和 `run_id` 管理；
- `TASK_START`、`CAR_STATE`、`CAR_POSE`、`TRACK_EVENT`、`CAR_DIAGNOSTIC`遥测；
- 累计里程达到7712 mm后的A点停车；起步不依赖A点标志可见；
- 丢线分级恢复和安全停车。

### 2.2 PyQt地面站

- 通过两路USB-UART连接两块ECB02；
- 分别解析飞机和小车直发的LXS1消息；
- 显示场地、飞机、小车、轨迹和C—D动作区；
- 显示全局状态、小车状态、动作状态和故障；
- 保存CSV或JSON日志；
- 统计链路超时和丢帧；
- 发送查询和紧急中止。

### 2.3 不在本计划内直接实现

- 天空端K230的小车和平台目标识别；
- 飞机飞控控制器和抛投机构；
- 凌霄飞控原厂协议适配；
- ECB02内部固件开发；
- 视频传输。

这些模块必须遵循统一状态机和LXS1接口完成联调。

## 3. 已确认硬件基线

| 模块 | 设备 | 接口 |
|---|---|---|
| 小车主控 | 天空星 STM32F407VET6 | 定时器、PWM、UART、DMA、SWD |
| 电机驱动 | 成品TB6612模块 | PWMA/PWMB、AIN1/AIN2、BIN1/BIN2、STBY |
| 驱动电机 | MG310MGR编码器电机 | 左右各A/B两相信号 |
| 小车视觉 | 亚博K230 | USART3，LXS1 `VISION_LINE` |
| 小车—飞机无线 | ECB02点对点 | USART2，115200 8N1 |
| 小车—地面站无线 | ECB02点对点 | USART1，115200 8N1 |
| 地面站 | Windows电脑 + PyQt | 两路USB-UART + 两块ECB02 |
| 飞机无线 | 两块ECB02 | 分别直连小车和地面站 |

当前小车引脚：

| 功能 | STM32引脚 |
|---|---|
| 左编码器（原右电机） | E1A→PB6、E1B→PB7 / TIM4 |
| 右编码器（原左电机） | E2A→PA5、E2B→PA1 / TIM2 |
| 本地任务按键 | 1×4共地按键板：PE5抛投、PE3动态起降、PC0开始、PC2中止复位 |
| 左电机PWM | PA8 / TIM1_CH1 |
| 左电机方向 | PB0、PB1 |
| 右电机PWM | PB15 / TIM12_CH2 |
| 右电机方向 | PB12、PB13 |
| TB6612 STBY | PB14 |
| 飞机无线串口 | PA2、PA3 / USART2 |
| 地面站无线串口 | PA9、PA10 / USART1 |
| 小车K230串口 | GPIO32/TX→PB11/RX，GPIO33/RX←PB10/TX / USART3 |

最新高强度底板的两轮中心轮距为 `432 mm`。左右电机已经交换，代码中的每圈计数、编码器符号、PWM通道和速度前馈参数均随原电机一起换边。

TB6612控制原则：

```text
正转：方向脚设置正向，PWM输出占空比
反转：方向脚设置反向，PWM输出占空比
滑行停止：PWM=0，方向脚为低
驱动待机：STBY=0
驱动使能：STBY=1
```

## 4. 任务模式与状态

任务模式使用编译常量：

```text
1 DROP             抛投任务
2 DYNAMIC_LANDING  动态起降任务
```

飞机全局流程：

```text
抛投：
IDLE → TAKEOFF → HOVER_3S → SEARCH → FOLLOW
→ DROP → RETURN → LAND_H → DONE

动态起降：
IDLE → TAKEOFF → SEARCH → FOLLOW
→ LAND_CAR → WAIT_5S → RETAKEOFF
→ RETURN → LAND_H → DONE
```

`FOLLOW` 负责伴飞并等待C点，不单独设置 `ACTION_WAIT_C`。

小车流程：

```text
READY → STARTING → NORMAL_TRACK
                    ↓ C_ENTER
                 ACTION_SLOW
                    ↓ RETURN或D_EXIT
                 NORMAL_TRACK
                    ↓ A_FINISH
               FINISH_CONFIRM → DONE
```

丢线流程：

```text
NORMAL_TRACK/ACTION_SLOW
→ LOST_HOLD
→ REACQUIRE
→ 返回丢线前状态

REACQUIRE超时 → FAULT
```

## 5. 赛道里程和速度

理论里程初值：

| 位置 | 累计里程 |
|---|---:|
| B点 | 1500 mm |
| C点 | 3856 mm |
| D点 | 5356 mm |
| 一圈 | 7712 mm |

统一配置参数：

```yaml
normal_speed_mm_s: 150
action_speed_mm_s: 80
reacquire_speed_mm_s: 40
odometry_calibration: 1.05187
point_b_path_mm: 1500
point_c_path_mm: 3856
point_d_path_mm: 5356
lap_path_mm: 7712
```

规则：

1. 按下小车按键后先保持电机关闭10 s，倒计时结束后若摄像头处于标志盲区，
   再以40 mm/s直行搜索最多3 s；识别到普通线后切换正常速度循线。
2. 到达C点后立即自主减速，不等待飞机或地面站确认。
3. C—D区间周期发送 `C_ENTER`。
4. 抛投完成后，飞机进入 `RETURN`，小车恢复正常速度。
5. 动态任务完成平台再起飞后，飞机进入 `RETURN`，小车恢复正常速度。
6. 若飞机链路失效，小车在D点或慢速超时后自主恢复正常速度。
7. 所有速度切换使用斜坡，动作区内不得停车。
8. 起步时看不到A点圆点和横线不影响启动；完成10秒安全倒计时并真正开始任务时，
   清零累计里程 `path_mm`。
9. B/C/D/A事件以地图累计里程为主判据：1500 mm触发B，3856 mm触发C并减速，
   5356 mm触发D并恢复正常速度，7712 mm触发A停车。
10. K230黑点字段仅用于画面诊断和标定，不参与任务点计数。黑点漏检、重复检测或
    误检都不得改变小车状态、速度或停车位置。

## 6. LXS1接口

### 6.1 小车K230 → 小车F407：VISION_LINE

```text
valid:u8
lost_count:u8
confidence:u16
lateral_error_mm:i16
heading_error_deg:i16
curvature:i16
marker_detected:u8
```

发送频率：20～50 Hz。超过视觉超时后不得继续使用旧误差。

### 6.2 小车F407 → 飞机：TASK_START

```text
run_id:u8
task_mode:u8
normal_speed_mm_s:u16
action_speed_mm_s:u16
```

小车按键锁存并完成10 s安全倒计时后发送。相同 `run_id` 的重复帧不得导致飞机重复起飞。

### 6.3 飞机 → 小车和地面站：TASK_STATE

```text
run_id:u8
task_mode:u8
global_state:u8
result:u8
fault_code:u16
elapsed_ms:u32
car_path_mm:u32
```

飞机周期发送。小车以 `global_state=RETURN` 作为正常恢复速度条件。

### 6.4 小车F407 → 飞机：CAR_STATE

```text
run_id:u8
car_state:u8
speed_mode:u8
line_valid:u8
speed_mm_s:u16
fault_code:u16
path_mm:u32
lap:u8
```

`lap=1`在 `path_mm >= 7712` 时置位，并立即触发 `A_FINISH`。

### 6.5 小车F407 → 飞机：CAR_POSE

```text
run_id:u8
x_mm:i16
y_mm:i16
heading_deg:i16
path_mm:u32
```

### 6.6 小车F407 → 地面站：CAR_DIAGNOSTIC

小车通过USART1以10 Hz直发固定52字节诊断数据，包含PC0开始键原始/消抖状态、
任务和巡线状态、左右请求/斜坡目标/实测速度、PWM、STBY、方向脚读回、编码器
累计值、故障位以及K230帧龄和格式错误计数。电脑端使用：

```powershell
pip install pyserial
python .\LXS1_通信协议\python\car_diagnostic_monitor.py --list
python .\LXS1_通信协议\python\car_diagnostic_monitor.py --port COM7
```

诊断脚本应直接给出按键、速度环、TB6612、电机输出、编码器或K230链路的接线建议。

### 6.6 小车F407 → 飞机：TRACK_EVENT

```text
run_id:u8
event:u8
path_mm:u32
```

事件编号：

```text
1 B_CROSS
2 C_ENTER
3 D_EXIT
4 A_FINISH
```

小车将状态、位置和事件同时直发飞机与地面站，两份消息使用相同 `run_id` 和负载。

## 7. 蓝牙通信计划

固定拓扑：

```text
飞机主控 ECB02-A ↔ 小车 ECB02-A
飞机主控 ECB02-B ↔ 地面站 ECB02-A
小车 USART1 + ECB02-B ↔ 地面站 ECB02-B
```

验证顺序：

1. 分别验证三对ECB02的点对点角色配置、自动重连和115200串口透传。
2. 验证三条链路同时工作时不会串帧，设备能按物理串口确认来源。
3. 验证任务消息100 ms周期下的延迟和丢包。
4. 断开任一地面站链路，确认飞机与小车仍能完成任务。

三个设备均固定使用两块ECB02，不再验证或依赖一主多从。

## 8. PyQt地面站架构

建议目录：

```text
GroundStation/
├─ main.py
├─ ui/
│  └─ main_window.py
├─ protocol/
│  ├─ lxs1_parser.py
│  └─ messages.py
├─ workers/
│  └─ serial_worker.py
├─ models/
│  └─ mission_model.py
├─ widgets/
│  └─ field_view.py
└─ tests/
```

线程模型：

```text
PyQt主线程
├─ 场地图和轨迹绘制
├─ 状态和告警
└─ 查询与紧急中止

串口工作线程
├─ pyserial或QSerialPort
├─ LXS1增量解析
├─ 设备超时判断
└─ 日志队列
```

第一版界面：

- 400 cm × 500 cm场地图；
- A/B/C/D/H点和C—D动作区；
- 飞机和小车实时位置及历史轨迹；
- 全局状态、小车状态和速度模式；
- 飞机高度、速度和视觉置信度；
- 距C/D点路程和动作区剩余时间；
- 蓝牙在线状态、最后收包时间和丢帧统计；
- 抛投、着陆、停留和再起飞结果；
- CSV/JSON日志；
- 查询和紧急中止按钮。

地面站离线时，飞机和小车必须继续任务。

## 9. 开发阶段

### P0：K230视觉与协议基线

- [x] QVGA灰度视觉；
- [x] 多ROI黑线识别；
- [x] 生成 `VISION_LINE`；
- [x] 协议字节测试；
- [x] 增加大黑圆点检测和A点终点事件；
- [ ] 实车光照和阴影测试。

验收：视觉稳定输出20 Hz以上，丢线时立即 `valid=0`，不输出旧控制量。

### P1：电机与编码器闭环

- [x] TB6612引脚配置；
- [x] 双编码器定时器配置；
- [x] 左右轮速度闭环骨架；
- [x] VOFA诊断；
- [ ] 完成150 mm/s速度标定；
- [ ] 验证80 mm/s动作慢速；
- [ ] 验证速度斜坡。

验收：正常速度和动作速度均稳定，切换时无冲击和停车。

### P2：循线与丢线恢复

- [x] 基于横向、航向和曲率的差速循线；
- [x] 短时丢线保持；
- [x] 长时低速重寻；
- [x] 恢复到丢线前速度状态；
- [x] 最终超时故障停车；
- [ ] 连续10圈稳定测试。

### P3：里程计和赛道事件

- [ ] 标定左右轮每计数路程；
- [ ] 标定有效轮距；
- [x] A点启动清零；
- [x] 按状态机初值实现B/C/D累计里程；
- [x] 发送 `B_CROSS/C_ENTER/D_EXIT`；
- [x] 累计里程达到7712 mm时触发A点停车；
- [x] 发送 `CAR_POSE`。

验收：连续多圈中C/D触发位置稳定，不能在启动位置误判完成。

### P4：小车任务状态机

- [x] 使用1×4任务按键板、20 ms非阻塞消抖及10 s安全撤离倒计时；
- [x] 编译时固定任务模式；
- [x] `run_id`递增和幂等；
- [x] C点自主减速；
- [x] `RETURN`、D点和超时三种恢复条件；
- [x] 一圈结束停车；
- [x] 中止和故障状态。

验收：不依赖地面站即可完成正常速度→动作慢速→正常速度→A点停车。

### P5：ECB02链路

- [x] 小车USART2—飞机点对点串口软件；
- [x] 小车USART1—电脑点对点串口软件；
- [ ] 飞机—电脑点对点配置；
- [ ] 三设备各双ECB02并发测试；
- [ ] 自动重连；
- [x] LXS1双向收发与增量解析；
- [x] 小车遥测分别直发飞机和地面站；
- [ ] 链路故障注入。

### P6：PyQt地面站

- [ ] 串口选择和连接；
- [ ] LXS1增量解析；
- [ ] 场地和轨迹显示；
- [ ] 三端状态显示；
- [ ] C—D动作区显示；
- [ ] 设备超时和丢帧统计；
- [ ] CSV/JSON日志；
- [ ] 紧急中止；
- [ ] PyQt离线打包。

### P7：系统联调

- [ ] 小车按键启动并发送 `TASK_START`；
- [ ] 飞机发布权威 `TASK_STATE`；
- [ ] B点在15秒内到达；
- [ ] C点全体进入抛投/降落阶段；
- [ ] 小车C点减速；
- [ ] 动作完成后恢复速度；
- [ ] D点超时取消动作；
- [ ] 飞机返回H点；
- [ ] 小车一圈不超过90秒；
- [ ] 地面站离线不影响任务；
- [ ] 重复消息不重复触发动作。

## 10. 验收清单

- [ ] 两种任务流程均与统一状态机一致；
- [ ] 不存在 `ACTION_WAIT_C` 状态；
- [ ] 小车在C—D区间只减速、不停车；
- [ ] 飞机从 `SEARCH/FOLLOW` 收到 `C_ENTER` 后进入动作状态；
- [ ] 抛投完成或平台再起飞完成后恢复正常速度；
- [ ] 越过D点未完成时取消动作并恢复速度；
- [ ] 地面站只显示、记录、查询和中止；
- [ ] 所有副作用消息按 `run_id` 幂等；
- [ ] Markdown、YAML、C、Python和PyQt使用相同状态编号；
- [ ] 设计报告记录实际速度、位置误差、任务时间和故障测试结果。
