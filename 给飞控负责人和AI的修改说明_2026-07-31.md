# 给飞控负责人和 AI 的修改说明

日期：2026-07-31  
适用范围：飞机 F407 主控、凌霄飞控适配端、天空端 K230 的通信与任务控制对接。

## 1. 先读本结论

本次协议修订确认：**距离计算放在飞机 F407 内完成。** 天空端 K230 只发送像素
测量，F407 从飞控获取实际高度并换算为相对距离，然后用于跟随、抛投和动态降落。

因此，`0x40` 的正式名称是 `VISION_PIXEL`，并且只能走：

```text
天空 K230 (0x03) -- UART -- 飞机 F407 (0x02)
```

它不应出现在 ECB02 链路、地面站、凌霄飞控串口或任务遥测中。报文仍为20字节完整帧，
`LEN=0x0F`、`MSG=0x40`、12字节 DATA、小端序；不要改 K230 现有帧长度或字段顺序。

权威协议文件：[LXS1_PROTOCOL.md](LXS1_通信协议/LXS1_PROTOCOL.md)。K230 解析和
首版计算参数见：[F407端_K230像素偏移协议与计算修改说明.md](../飞机视觉的代码/F407端_K230像素偏移协议与计算修改说明.md)。

## 2. `VISION_PIXEL 0x40` 的唯一解释

```text
0      valid:u8
1      target_kind:u8      1 小车，2 小车起降平台
2..3   confidence:u16      0..1000，小端序
4..5   dx_px:i16          图像右方为正
6..7   dy_px:i16          图像下方为正
8..9   radius_px:i16      外圆半径，像素
10..11 vision_mode:i16    1 ACQUIRE，2 TRACK，3 LOST
```

处理规则：

- `valid=0`仍是合法新帧，更新视觉接收时间；但不得用于运动控制。
- `valid=0`时 K230 必须将 `dx_px/dy_px/radius_px`置零，F4 不得复用旧误差。
- 视觉帧超过300 ms未更新，按无效处理；跟踪控制可使用更严格的120 ms新鲜度门限。
- K230 心跳不能刷新视觉数据时间戳。
- 现有 K230 脚本固定输出 `target_kind=2`，用于平台；要跟小车时再经双方确认后扩展为
  `target_kind=1`，不能靠猜测字段含义。

## 3. F407 必须新增的像素→距离适配层

F407 保存两份数据，禁止把像素帧直接交给控制器：

```text
VisionPixel       K230原始数据、接收时间
VisionRelative    F407换算后的FRD厘米误差、所用高度时间戳、有效标志
```

首版计算按已确认的相机安装方向和参数：

```text
camera_height_cm = laser_height_cm - 0.75
x_frd_cm = -dx_px * camera_height_cm / 365.0
y_frd_cm = -dy_px * camera_height_cm / 365.0 + 1.6
z_frd_cm = camera_height_cm
```

其中 `laser_height_cm`必须来自飞控的实际、高度新鲜的状态，不得用固定135 cm，也不能
用上一次高度永久外推。高度或飞控状态超过500 ms未更新时，F407停止下发新的运动指令，
转入飞控安全策略。

当前图像方向已约定：图像右=飞机后、图像下=飞机左，因此前述两个负号不可省略。
若现有 F407 控制内部仍使用“Y 向左为正”，只在控制适配层做：

```text
control_x = x_frd_cm
control_y_left = -y_frd_cm
```

不要把这种内部坐标约定写回 LXS1 字段。

## 4. F407 是任务唯一权威，凌霄是执行与安全层

F407 不应继续按开机固定航点自动开始。它必须等待小车发来的新 `TASK_START(run_id)`，
对同一 `run_id`幂等初始化，再执行统一状态机：

```text
抛投：IDLE → TAKEOFF → HOVER_3S → SEARCH → FOLLOW → DROP → RETURN → LAND_H → DONE
动态：IDLE → TAKEOFF → SEARCH → FOLLOW → LAND_CAR → WAIT_5S → RETAKEOFF → RETURN → LAND_H → DONE
```

关键条件：

- 起飞和复飞到 `150 ± 10 cm`；抛投模式需要稳定悬停3秒。
- `C_ENTER`在 `SEARCH` 时也必须进入 `DROP/LAND_CAR`；动作前半段可以继续搜索和对准。
- `DROP`必须持续伴飞：F407持续匹配小车的前向速度、横向位置和航向。达到连续稳定的
  相对误差门限时，在小车仍前进、飞机仍跟随的条件下单次释放；释放前后不得改为定点
  悬停、速度清零或等待小车。
- 收到 `D_EXIT`或动作超时而尚未完成释放/着陆时，取消动作并进入`RETURN`。
- 抛投、开始下降、释放执行器、再起飞只能在状态入口执行一次；重复`C_ENTER`不得重复触发。
- 每100 ms向小车、地面站发送`TASK_STATE`。

凌霄端不得再拥有独立的“延时起飞—悬停—固定任务”流程；它只执行 F407 下发的动作，
反馈姿态/高度/故障，并在指令失联时安全悬停或按自身失效保护处理。

## 5. 飞机侧必须完成的通信接口

| 接口 | F407要做什么 | 周期/超时 |
|---|---|---|
| K230 本地 UART | 只收`VISION_PIXEL 0x40`，解析后换算 | 10～20 Hz；视觉300 ms失效 |
| 小车 ECB02 | 收`TASK_START`、`CAR_STATE`、`CAR_POSE`、`TRACK_EVENT`；发`TASK_STATE` | 遥测/状态100 ms；C区重复事件100 ms |
| 地面站 ECB02 | 发`TASK_STATE`、`FC_STATE`、`FC_POSE`、`DROP_STATE`、`LAND_STATE`、`FAULT`；收`TASK_ABORT` | `TASK_STATE`100 ms |
| 凌霄 UART | 把 F407 内部命令转换为原厂协议；接收高度、姿态和故障 | 飞控状态500 ms失效 |

三条 UART 的接收中断只负责搬运至各自缓冲区；帧解析、状态机和发送放在主循环/任务中。
两块 ECB02 必须使用两路独立 UART，不能把小车、地面站和 K230 混到同一接收缓冲。

## 6. 凌霄适配端必须先修的安全项

1. 为 F407→凌霄的运动命令增加超时看门狗；超时后速度目标清零并进入安全悬停/原厂保护，
   禁止持续执行最后一帧非零命令。
2. 修复任务完成帧的解析状态机：只有完整、顺序正确的 `FD 01 0D` 才能确认完成，任何
   单个普通字节都不能推进到“已收到帧头”。
3. PID参数只在初始化时装载；不要每20 ms重复 `PID_Init()`。同时修正微分项计算顺序，
   先使用旧值计算，再更新旧值。
4. F407 对飞控状态超过500 ms不再发新动作；凌霄端仍要自行兜底，不能依赖 F407 永远在线。

## 7. 推荐实施顺序与验收

1. 先接 K230 与 F407：打印原始像素、实际高度、换算出的`x_frd/y_frd`；人工移动平台，
   验证左右、前后均不反向。
2. 再接凌霄但不装桨：验证高度新鲜度、500 ms失联安全行为和零速度命令。
3. 接小车链路：回放`TASK_START/C_ENTER/D_EXIT`，确认`run_id`幂等和状态转移。
4. 接地面站：确认100 ms `TASK_STATE`与`FC_STATE/FC_POSE`完整显示；断开地面站不影响任务。
5. 最后撤桨全流程、系留低高度、正式场地测试。回放/实测中要确认DROP期间小车不停、
   飞机持续输出匹配小车速度的命令；任何动作未在D点前完成，必须返航而不是继续尝试。

## 8. 不要做的事

- 不要把 `0x40`改回厘米级`VISION_TARGET`，也不要把`radius_px/vision_mode`解释成`dz/yaw`。
- 不要单端给LXS1增加CRC、ACK、字段或消息号；当前版本无CRC，升级必须所有节点同步。
- 不要让地面站参与正常任务决策或依赖它转发小车—飞机消息。
- 不要因收到重复`TASK_START`或`C_ENTER`重复解锁、投放、下降或起飞。
