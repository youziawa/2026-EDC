# 飞行器联调通信对接汇报

日期：2026-07-30  
对接对象：飞行器控制负责人、飞行器视觉负责人  
协议基线：LXS1 v1.1  
状态机基线：`land_air_state_machine.yaml`

## 1. 当前小车侧已经完成

- STM32F407小车主控：按键触发后10秒安全倒计时、循线、双轮速度闭环、里程计、
  状态机、两路ECB02链路和LXS1收发。
- K230小车视觉：40 Hz黑线横向误差、航向误差和曲率输出；直道控制已做死区、
  曲率降权和修正斜率限制。
- 地图里程事件：B=1500 mm、C=3856 mm、D=5356 mm、A=7712 mm。
- C—D区间自动从150 mm/s减速至80 mm/s，D点恢复，A点停车。
- 整圈实测越过A点约400 mm的问题已用 `odometry_calibration=1.05187`校准。
- 小车圆点识别仅保留诊断用途，不参与B/C/D/A任务推进。
- 小车USART2直连飞机，USART1直连地面站，USART3连接小车K230。
- 可用电脑端诊断工具已经提供；正式PyQt地面站仍待实现。

## 2. 两位负责人共同遵守的通信基线

所有LXS1链路默认 `115200 8N1`，多字节数据为小端序：

```text
AA 55 | LEN | SRC | DST | MSG | DATA | 0D 0A
```

节点地址：

```text
0x01 飞控适配端
0x02 飞机主控
0x03 天空端K230
0x04 小车主控
0x05 小车K230
0x06 地面站
0xFF 广播
```

ECB02只能点对点，不能一主多从。飞机、小车、地面站各使用两块模块，共三条链路：

```text
飞机 ↔ 小车
飞机 ↔ 地面站
小车 ↔ 地面站
```

飞机端必须准备两路独立UART分别连接两块ECB02，不能在一块ECB02上等待两个从机。
飞机与天空端K230还需要第三路本地UART。

完整字段定义以 [LXS1_PROTOCOL.md](LXS1_通信协议/LXS1_PROTOCOL.md) 为准。

## 3. 飞行器控制负责人需要处理的信息

### 3.1 必须接收小车消息

#### TASK_START `0x10`

方向：小车 `0x04` → 飞机 `0x02`  
长度：6字节

```text
0 run_id:u8
1 task_mode:u8             1抛投，2动态起降
2..3 normal_speed_mm_s:u16
4..5 action_speed_mm_s:u16
```

处理要求：

- 只对新 `run_id`初始化一次任务并起飞。
- 同一 `run_id`重复到达时不得重复解锁、起飞、投放或降落。
- 收到后根据编译/赛前指定模式进入对应全局流程。

#### TRACK_EVENT `0x33`

方向：小车 → 飞机  
长度：6字节

```text
0 run_id:u8
1 event:u8       1=B_CROSS，2=C_ENTER，3=D_EXIT，4=A_FINISH
2..5 path_mm:u32
```

处理要求：

- `C_ENTER`是飞机执行抛投或动态降落的关键触发。
- C—D区间内小车每100 ms重复发送 `C_ENTER`，飞机必须按 `run_id`幂等。
- 抛投模式：`SEARCH/FOLLOW → DROP`。
- 动态起降：`SEARCH/FOLLOW → LAND_CAR`。
- 到D点仍未完成动作时取消动作并返航。
- `A_FINISH`表示小车已完成一圈并停车，不等于飞机可以忽略自身返航/降落状态。

#### CAR_STATE `0x31`

方向：小车 → 飞机，100 ms  
长度：13字节

```text
0 run_id
1 car_state
2 speed_mode      0停止，1正常，2动作慢速，3搜索/恢复
3 line_valid
4..5 speed_mm_s
6..7 fault_code
8..11 path_mm
12 lap
```

用途：判断小车是否处于C—D慢速平台区、是否故障、是否完成。

#### CAR_POSE `0x32`

方向：小车 → 飞机，100 ms  
长度：11字节

```text
0 run_id
1..2 x_mm:i16
3..4 y_mm:i16
5..6 heading_deg:i16
7..10 path_mm:u32
```

用途：天空视觉短时丢失时的搜索先验和地面站轨迹显示。里程坐标会有轮滑累计误差，
不能替代天空视觉的最终对准。

### 3.2 必须发送给小车的消息

#### TASK_STATE `0x12`

方向：飞机 → 小车、地面站  
周期：100 ms  
长度：14字节

```text
0 run_id
1 task_mode
2 global_state
3 result
4..5 fault_code
6..9 elapsed_ms
10..13 car_path_mm
```

全局状态码必须固定为：

```text
0 IDLE       1 TAKEOFF     2 HOVER_3S   3 SEARCH
4 FOLLOW     5 DROP        6 LAND_CAR   7 WAIT_5S
8 RETAKEOFF  9 RETURN     10 LAND_H    11 DONE
12 ABORT    13 FAULT
```

小车目前会校验 `SRC=0x02`、飞机专用物理链路和 `run_id`。收到 `RETURN=9`时，
若仍在动作慢速会恢复正常速度。`TASK_STATE`必须周期发送，不能只发一帧。

#### TASK_ABORT `0x11`

方向：飞机/地面站 → 广播  
长度：至少2字节

```text
0 run_id
1 reason
```

飞机进入安全降落流程，小车立即停车。旧 `run_id`不得中止新任务。

### 3.3 飞机内部还需要完成

- 凌霄飞控原厂协议适配层：LXS1 `FC_CMD/FC_STATE/FC_POSE`与飞控原厂协议互转。
- 抛投执行器状态：发送 `DROP_STATE 0x50`到地面站。
- 动态降落状态：发送 `LAND_STATE 0x51`到地面站。
- 故障：发送 `FAULT 0x60`，并在 `TASK_STATE.fault_code`保持当前故障。
- 飞控状态超过500 ms未更新时停止下发新的运动指令并采用飞控安全策略。
- 地面站离线不能中断飞机和小车的正常任务。

## 4. 飞行器视觉负责人需要处理的信息

天空端K230节点地址为 `0x03`，只向飞机主控 `0x02`输出测量，不直接控制飞控。

坐标统一采用飞机机体系FRD：

```text
X：机头前方为正
Y：机体右方为正
Z：机体下方为正
yaw：沿Z轴向下看，顺时针为正
距离：cm
置信度：0..1000
```

如果飞控内部使用NED、ENU或其他坐标，转换应放在飞机主控适配层，不能让同一
LXS1字段在不同状态使用不同正方向。

### 4.1 VISION_TARGET `0x40`

方向：天空端K230 → 飞机  
频率：10～20 Hz  
长度：12字节

```text
0 valid:u8
1 target_kind:u8        1小车，2小车起降平台
2..3 confidence:u16
4..5 dx_cm:i16          前方为正
6..7 dy_cm:i16          右方为正
8..9 dz_cm:i16          下方为正
10..11 yaw_error_deg:i16
```

用途：

- `SEARCH`：搜索并锁定小车/平台。
- `FOLLOW`：给出跟随位置和航向误差。
- `LAND_CAR`：给出车载平台相对位置，供下降前水平闭环。

### 4.2 VISION_LANDMARK `0x41`

方向：天空端K230 → 飞机  
频率：10～20 Hz  
长度：10字节

```text
0 valid:u8
1 marker_kind:u8        1返航H点，2投放标志，3车载降落标志
2..3 confidence:u16
4..5 error_x_cm:i16
6..7 error_y_cm:i16
8..9 error_yaw_deg:i16
```

用途：

- 抛投阶段的投放标志对准。
- 动态降落阶段的平台精对准。
- `RETURN/LAND_H`阶段的H点识别与降落对准。

### 4.3 视觉输出硬要求

- `valid=0`时所有误差字段必须清零，不得输出上一帧残留值。
- 不允许用 `confidence>0`代替 `valid`。
- 飞机主控以接收时刻判断新鲜度；超过300 ms按视觉无效。
- 输出必须连续，不能“识别到才发”；无目标时也要周期发送 `valid=0`。
- 建议飞机主控要求连续有效帧再从SEARCH进入FOLLOW或开始下降。
- 抛投/下降是飞机状态机单次副作用，视觉K230不得直接触发执行器。

Python侧已有可复用打包函数：

```python
from lxs1 import pack_vision_target, pack_vision_landmark
```

## 5. 联调顺序

1. 不接电机/螺旋桨，三端验证帧头、LEN、SRC/DST、消息号和小端序。
2. 飞机主控发送周期 `TASK_STATE`，小车确认只接受飞机链路和 `SRC=0x02`。
3. 小车按键启动，确认飞机收到新 `run_id`的 `TASK_START`。
4. 人工注入B/C/D/A里程事件，确认C触发DROP/LAND_CAR、D未完成则RETURN。
5. 天空K230静态标定FRD坐标正方向和 `valid=0`清零。
6. 撤下螺旋桨验证完整状态流和幂等，再进行系留/低高度测试。
7. 最后验证地面站断开后任务仍可完成，`TASK_ABORT`仍可从任一有效链路触发。

## 6. 验收清单

- [ ] 两块飞机ECB02分别占用两路UART，第三路UART连接天空K230。
- [ ] 同一 `run_id`重复 `TASK_START/C_ENTER`不重复触发副作用。
- [ ] `TASK_STATE`每100 ms发送，状态码与本报告完全一致。
- [ ] `C_ENTER`在SEARCH状态也能进入DROP或LAND_CAR。
- [ ] D点动作未完成时取消动作并RETURN。
- [ ] 天空视觉无目标时持续发送 `valid=0`且误差全0。
- [ ] 天空视觉FRD轴向通过人工移动目标验证。
- [ ] 视觉断流300 ms和飞控断流500 ms安全策略有效。
- [ ] 地面站离线不影响正常任务。

## 7. 仓库入口

- [统一状态机](land_air_state_machine.yaml)
- [队员版状态机](陆空协同统一状态机.md)
- [LXS1协议](LXS1_通信协议/LXS1_PROTOCOL.md)
- [C协议实现](LXS1_通信协议/include/lxs1_protocol.h)
- [Python协议实现](LXS1_通信协议/python/lxs1.py)
- [小车STM32工程](Car/STM32/CarF407)
- [小车K230工程](Car/K230)
- [小车硬件与接线](Car/Hardware)

## 8. 当前未完成或需要现场确认

- 飞机端两块ECB02和天空K230的具体UART编号、TX/RX引脚尚需飞机负责人填写。
- 凌霄飞控原厂协议适配层、飞机状态机和动作执行器代码不在本次小车提交中。
- 天空端K230的检测算法与串口发送代码需要飞行器视觉负责人实现。
- 正式PyQt地面站尚未完成；当前只有小车串口诊断监视器。
- LXS1当前没有CRC。台架联调可用；若现场串口干扰明显，三方必须一起升级协议版本，
  不能单端私自增加校验字节。
